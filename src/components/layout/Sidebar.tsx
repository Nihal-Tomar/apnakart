'use client';

export default function Sidebar({ isOpen, onClose }: { isOpen: boolean; onClose: () => void }) {
  // Sidebar removed in favor of horizontal navigation.
  // Kept as an empty component for import compatibility.
  return null;
}
